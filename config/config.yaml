# KAPI 配置文件

# vLLM 服务配置
vllm:
  model_name: "mistralai/Mistral-7B-Instruct-v0.2"
  api_base: "http://localhost:8000/v1"
  api_key: "EMPTY"
  temperature: 0.1
  max_tokens: 2048

# 解析器配置
parser:
  use_few_shot: true
  validate_output: true

# 日志配置
logging:
  level: "INFO"
